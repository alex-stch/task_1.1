#!/bin/bash

cat > pkg_create.json << EOF
{
  "name": "${TRAVIS_BRANCH}",
  "licenses": ["GPL-3.0-or-later"],
  "vcs_url": "-",
}
EOF

curl --verbose -ualex-stch:$BINTRAY_API_KEY --header "Content-Type: application/json" --data @pkg_create.json https://api.bintray.com/packages/alex-stch/otuscpp

#rm -fv pkg_create.json